version: '3.8'
services:
  redis:
    image: redis:latest
    ports:
      - "6380:6379"
    restart: unless-stopped

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    restart: unless-stopped
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
