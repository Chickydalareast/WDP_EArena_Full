import { Metadata } from "next";
export declare const metadata: Metadata;
export default function CourseSettingsPage({ params }: {
    params: Promise<{
        courseId: string;
    }>;
}): Promise<any>;
