interface ReplyReviewModalProps {
    courseId: string;
    reviewId: string | null;
    onClose: () => void;
}
export declare const ReplyReviewModal: ({ courseId, reviewId, onClose }: ReplyReviewModalProps) => any;
export {};
