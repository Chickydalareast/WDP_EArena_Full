interface MatrixBuilderDrawerProps {
    isOpen: boolean;
    onClose: () => void;
    paperId: string;
    subjectId: string;
}
export declare function MatrixBuilderDrawer({ isOpen, onClose, paperId, subjectId }: MatrixBuilderDrawerProps): any;
export {};
